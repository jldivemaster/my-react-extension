class Resource < ApplicationRecord
  # Links to external resources relevant by Mod, subject matter.
  # All users can view resources, but only those with Admin = true (instructors) can create new resources.

end
